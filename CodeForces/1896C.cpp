#include<bits/stdc++.h>
using namespace std;
d(vector<int>a)
{
    
}
int main()
{
    long long int t;
    cin>>t;
    while(t--)
    {
        long long int n;
        cin>>n;
        vector<long long int>a(n,0);
        for(long long int i=1;i<=n;i++)
        {
            cin>>a[i];
        }
        d(a);
    }
} 

